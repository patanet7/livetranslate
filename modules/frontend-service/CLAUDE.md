# Frontend Service (Legacy)

**Status**: Replaced by `modules/dashboard-service/` (SvelteKit + Svelte 5). Retained only for settings management pages not yet migrated.

## Commands

```bash
cd modules/frontend-service
npm install
npm run dev       # Dev server on :5173
npm test          # Vitest unit tests
```

## Note

The primary UI is `modules/dashboard-service/`. Do not add new features here.
