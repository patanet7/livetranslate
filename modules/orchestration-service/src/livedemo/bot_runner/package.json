{
  "name": "livedemo-bot-runner",
  "version": "0.1.0",
  "description": "Thin Playwright runner for the livedemo pipeline (joins Meet, draws frames from canvas WS).",
  "private": true,
  "type": "module",
  "main": "src/runner.ts",
  "scripts": {
    "start": "tsx src/runner.ts",
    "build": "tsc -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "playwright": "^1.45.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
